<body>
    <div class="container tm-mt-big tm-mb-big">
        <div class="row">
            <div class="col-12 mx-auto">
                <div class="tm-bg-primary-dark tm-block tm-block-h-auto">
                    <div class="row">
                        <div class="col-12">
                            <h2 class="tm-block-title d-inline-block">Edit Product</h2>
                            <button id="editButton" class="btn btn-primary float-right">Edit Product</button>
                        </div>
                    </div>
                    <div class="row tm-content-row">
                        <div class="col-12">
                            <div id="notification" class="alert" style="display: none;"></div>
                            <form id="editProductForm" enctype="multipart/form-data" class="tm-edit-product-form">
                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="form-group mb-3">
                                            <label for="productName">Product Name</label>
                                            <input id="productName" name="productName" type="text" class="form-control validate" required disabled value="{{product.productName}}" />
                                        </div>
                                        <div class="form-group mb-3">
                                            <label for="description">Description</label>
                                            <textarea id="description" name="description" class="form-control validate" rows="3" required disabled>{{product.description}}</textarea>
                                        </div>
                                        <div class="form-group mb-3">
                                            <label for="category">Category</label>
                                            <select id="category" name="category" class="form-control validate" required disabled>
                                                <option value="" disabled>Select a category</option>
                                                {{#each categories}}
                                                    <option value="{{this}}" {{#if (eq this ../product.category)}}selected{{/if}}>{{this}}</option>
                                                {{/each}}
                                            </select>
                                        </div>
                                        <div class="form-group mb-3">
                                            <label for="manufacturer">Manufacturer</label>
                                            <select id="manufacturer" name="manufacturer" class="form-control validate" required disabled>
                                                <option value="" disabled>Select a manufacturer</option>
                                                {{#each manufacturers}}
                                                    <option value="{{this}}" {{#if (eq this ../product.manufacturer)}}selected{{/if}}>{{this}}</option>
                                                {{/each}}
                                            </select>
                                        </div>
                                        <div class="form-group mb-3">
                                            <label for="price">Price</label>
                                            <input id="price" name="price" type="number" step="0.01" class="form-control validate" required disabled value="{{product.price}}" />
                                        </div>
                                        <div class="form-group mb-3">
                                            <label for="rating">Rating (1-5) <i class="fas fa-star"></i> </label>
                                            <input id="rating" name="rating" type="number" class="form-control validate" required disabled value="{{product.rating}}" />
                                        </div>
                                        <div class="form-group mb-3">
                                            <label>Status</label>
                                            <div>
                                                <label class="radio-inline">
                                                    <input type="radio" name="status" value="On stock" onclick="toggleStockQuantity(true)" required disabled {{#if (eq product.status 'On stock')}}checked{{/if}}> On stock
                                                </label>
                                                <label class="radio-inline">
                                                    <input type="radio" name="status" value="Out of stock" onclick="toggleStockQuantity(false)" disabled {{#if (eq product.status 'Out of stock')}}checked{{/if}}> Out of stock
                                                </label>
                                                <label class="radio-inline">
                                                    <input type="radio" name="status" value="Suspend" onclick="toggleStockQuantity(false)" disabled {{#if (eq product.status 'Suspend')}}checked{{/if}}> Suspend
                                                </label>
                                            </div>
                                        </div>
                                        <div class="form-group mb-3" id="stockQuantityGroup" style="display: {{#if (eq product.status 'On stock')}}block{{else}}none{{/if}};">
                                            <label for="stock_quantity">Stock Quantity</label>
                                            <input id="stock_quantity" name="stock_quantity" type="number" class="form-control validate" disabled value="{{product.stock_quantity}}" />
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="form-group mb-3 d-flex align-items-center justify-content-between">
                                            <label for="thumbnail" class="mb-0">Thumbnail</label>
                                            <button type="button" class="btn btn-primary" onclick="document.getElementById('thumbnail').click();" disabled>Upload Thumbnail</button>
                                            <input id="thumbnail" name="thumbnail" type="file" class="form-control validate" accept="image/*" style="display: none;" onchange="previewThumbnail(event)" disabled />
                                        </div>
                                        <img id="thumbnailPreview" src="{{product.url}}" alt="Thumbnail Preview" class="img-fluid mt-2" style="max-width: 100px;" />
                                        <div class="form-group mb-3 d-flex align-items-center justify-content-between">
                                            <label for="images" class="mb-0">Images (Can upload multiple)</label>
                                            <button type="button" class="btn btn-primary" onclick="document.getElementById('images').click();" disabled>Upload Images</button>
                                            <input id="images" name="images" type="file" class="form-control validate" accept="image/*" multiple style="display: none;" onchange="previewImages(event)" disabled />
                                        </div>
                                        <div id="imagesPreview" class="mt-2">
                                            {{#each product.urls}}
                                                <img src="{{this}}" alt="Image Preview" class="img-fluid mt-2" style="max-width: 100px; margin-right: 10px;" />
                                            {{/each}}
                                        </div>
                                    </div>
                                    <div class="col-12">
                                        <button type="submit" class="btn btn-primary btn-block text-uppercase" disabled>Save Changes</button>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="/js/editProduct.js"></script>
</body>